# 更新日志

## Unreleased

### 设置体验

- 收敛系统设置里的供应商、模型目录、图生文和提示词润色入口：供应商与模型成为端点、凭证、模型读取和能力标注的主入口，模型目录下沉为模型能力检视。
- 供应商与模型新增“供应商端点管理”统一入口，集中维护文本、图片、视频端点、API Key、Base URL 和模型白名单；图片/视频端点管理变为分类视图，端点管理和能力审计不再混在同一页。
- 图片端点、图生文兼容端点和视频供应商端点统一改用“获取模型列表”弹窗管理模型，支持读取后多选、全选当前结果、已添加自动选中和手动补充隐藏模型。
- 视频供应商端点按供应商模板拆成独立可折叠区块，每个端点复用统一端点卡片，只保留凭证、Base URL、模型纳管摘要和统一弹窗入口。
- 图生文与多模态页面改为任务设置页，只保留默认模型、输出格式、流式/结构化输出、系统提示词和历史开关；旧专用连接降级为兼容入口。
- 提示词润色配置拆清润色默认模型和批量规划默认模型，并复用统一端点管理中的文本端点和模型；旧 API Key/Base URL/裸模型 ID 仅作为迁移或临时覆盖兜底。

### 图片尺寸

- 优化 `gpt-image-2` 自定义尺寸推荐：超过 OpenAI `3:1` 比例上限的输入会明确提示被夹到合规比例，不再只给一个看起来比例错误的尺寸。
- 自定义尺寸推荐改为多候选，覆盖更小、接近和更大的合规分辨率档位，方便在 1K 到 4K 范围内选择。

## v2.12.4 - 2026-05-23

### 稳定性与错误处理

- 修复分享弹窗启用“使用密码加密整个分享链接”后，生成的分享链接不会随密码确认实时更新的问题；两次解密密码一致后会自动生成 `sdata` 加密链接，创建短链时也会使用当前加密链接。
- 修复多域名部署下短链和管理后台认证绑定到固定生产域名的问题；短链地址现在跟随实际请求域名，后台认证按请求域名使用同一套账号数据库。

### 文档与发布

- 本次发布同步更新 Web、package-lock、Tauri 与 Cargo 版本号到 `2.12.4`，用于触发 `v2.12.4` Release。

### 升级注意事项

- 本次无破坏性变更。发布后建议重点验证：密码加密分享链接确认后自动刷新、加密短链创建、两套线上域名的短链跳转，以及管理后台登录链路。

## v2.12.3 - 2026-05-23

### 重点更新

- 统一供应商设置新增视频供应商端点模板，覆盖 OpenAI Sora、Google Veo、Runway、Luma、MiniMax Hailuo、Kling、BytePlus ModelArk、DashScope / Wan、Tencent Hunyuan / TokenHub、fal.ai 和 xAI，新增端点后可以直接进入模型选择流程。
- 模型选择流程支持远端读取、搜索、勾选当前结果、清空选择和批量添加，同时保留手动添加模型 ID、显示名和上游厂商，便于接入隐藏模型、私有模型和未返回模型。
- 视频模型会写入统一模型目录并按端点协议推断能力；空视频端点默认带空白名单并显示待补模型，只有绑定真实可用模型后才会进入视频任务默认选择。

### 稳定性与错误处理

- Web `/api/provider-models` 与 Tauri `proxy_provider_models` 均允许 `openai-videos`、`tencent-tokenhub-video` 等可复用 OpenAI-compatible `/models` 的视频协议读取模型列表。
- OpenAI Sora 与 DashScope / Wan 继续作为已实现视频协议参与默认绑定，其他协议保持“协议预留 / 适配器待实现”状态，避免占位协议被误认为已可提交生成。
- 升级 Next.js 到 `16.2.6`，并通过 npm overrides 固定安全版 `postcss` 与 `brace-expansion`，清理当前 npm 依赖安全告警。

### 文档与发布

- 新增视频供应商与模型接入补齐需求文档，记录视频供应商可见性、模型发现、多选添加、手动添加和默认绑定闭环。
- 新增依赖漏洞修复与依赖安全全面排查需求文档，记录 Next.js、PostCSS、brace-expansion 的审计基线、修复要求和后续治理项。
- 本次发布同步更新 Web、package-lock、Tauri 与 Cargo 版本号到 `2.12.3`，用于触发 `v2.12.3` Release。

### 升级注意事项

- 本次无破坏性变更。发布后建议重点验证：新增视频供应商端点、视频模型远端读取与批量添加、手动添加视频模型、空端点待补模型提示，以及依赖升级后的 Web、桌面静态导出和生产部署链路。

## v2.12.2 - 2026-05-23

### 重点更新

- 历史面板与云同步设置新增更细的同步范围控制，支持分别管理图生文历史、视频历史、源图、缩略图和视频文件等数据类型，视频文件默认不自动同步，降低带宽与对象存储成本。
- 展示内容后台补充 PC、移动端和紧凑入口三类展示位的推荐尺寸、比例和安全区提示，前台轮播改为完整显示素材，减少按推荐规格设计的图片被裁切。
- Seedream 5 接入新增 Ark 图片生成端点支持，并补充 `doubao-seedream-5-0-260128` 模型元数据与高级参数，让 Web 服务器中转和 Tauri 桌面中转都能使用当前选择的 Seedream 端点凭据。

### 稳定性与错误处理

- 分享页 DevTools 抑制会在页面清理分享参数前捕获初始 URL，避免分享保护因为 URL 参数被提前移除而失效。
- 提示词草稿恢复只提示有实际内容的草稿，空字符串或纯空白草稿不会再显示“0 字符”恢复条。
- 公开展示内容会校验关联分享 Key 的启用、撤销和过期状态，禁用或过期的分享 Key 不再让对应推荐内容继续对外可见。
- 图生文和视频模式下批量入口保持可见但禁用，提示词快捷工具栏在首帧前完成右侧对齐，并补齐视频模型选择相关翻译 key。

### 文档与发布

- 同步更新分享与同步、供应商设置、桌面部署、工作台和展示内容后台文档，补充 Seedream 5 Ark 端点、展示素材规格和同步范围说明。
- 本次发布同步更新 Web、package-lock、Tauri 与 Cargo 版本号到 `2.12.2`，用于触发 `v2.12.2` Release。

### 升级注意事项

- 本次无破坏性变更。发布后建议重点验证：历史云同步范围保存与恢复、Seedream 5 Ark 端点生成、展示位素材完整显示、禁用/过期分享 Key 的展示内容不可见，以及分享页 DevTools 抑制是否按预期启动。

## v2.12.1 - 2026-05-22

### 重点更新

- 分享临时 API Key 时新增“禁止批量能力”选项并默认开启，接收方打开分享后批量入口会保持可见但不可用，避免分享额度被一次性批量消耗。
- 短链创建允许保存带 `#key=` 解密密码的加密分享链接，后台继续记录风险标记，便于用户按实际分享场景决定是否携带解密密码。
- 提示词下方工具栏改为右侧优先显示，超出宽度时默认滚动到最右侧；视频按钮默认隐藏，可在系统设置的提示词按钮配置中重新开启。

### 稳定性与错误处理

- Seedream 新增 `doubao-seedream-5-0-260128` 模型元数据与高级参数支持，并让 Web 服务器中转和 Tauri 桌面中转一致使用当前选择的 Seedream 端点凭据；文档补充 Coding Plan / Agent Plan 工具接口不能当作图片生成 Base URL。
- 刷新页面后不再自动恢复并展示上一轮批量任务预览，批量规划草稿仍可在批量面板内手动恢复，避免旧预览在新会话中残留。
- 分享入口默认启用 DevTools 抑制，并在页面清理分享参数前捕获初始 URL，避免分享页保护因 URL 参数被提前移除而没有启动。
- 提示词草稿恢复只提示有实际内容的草稿，空字符串或纯空白草稿不会再显示“0 字符”恢复条。
- 图生文和视频模式下批量按钮不再隐藏，而是以禁用状态展示，后续相关能力支持批量后可直接恢复启用。
- 提示词快捷工具栏在首帧前完成右侧对齐，避免进入页面时先显示在左侧再跳到右侧。

### 文档与发布

- 同步更新分享、短链、提示词工具栏、视频按钮和批量规划相关文档，并补充对应解析、加密分享、短链、DevTools 抑制和草稿恢复测试覆盖。
- 补充三处展示位的 PC/移动端推荐尺寸、推荐比例和安全区说明；展示图改为完整显示，避免按推荐比例设计的素材被裁切。
- 本次发布同步更新 Web、package-lock、Tauri 与 Cargo 版本号到 `2.12.1`，用于触发 `v2.12.1` Release。

### 升级注意事项

- 本次无破坏性变更。发布后建议重点验证：带解密密码短链创建、临时 API Key 分享接收端批量禁用、刷新后批量预览不自动恢复，以及提示词工具栏在移动端和桌面端的右侧优先显示。

## v2.12.0 - 2026-05-22

### 重点更新

- 分享弹窗新增短链创建流程，必须由用户明确点击创建按钮才会生成短链，不再自动创建，避免批量产生无用链接。
- 管理后台新增短链管理页，可查看短链列表、访问情况，并为短链关联不同的推荐内容。
- 短链功能支持后台总开关、创建口令、长度与安全策略控制，便于按需开放并降低误用风险。

### 稳定性与错误处理

- 短链访问统计改为服务端管理并做更保守的记录处理，减少敏感信息暴露面。
- 对短链创建、目标跳转和外部链接策略增加了更严格的校验，提升开放分享场景下的安全性。

### 文档与发布

- 新增并完善 `docs/requirements/SHORT_LINK_SHARING_REQUIREMENTS.md`，同步更新分享与管理后台相关文档。
- 本次发布同步更新 Web、Tauri 与桌面版本号到 `2.12.0`，用于触发 `v2.12.0` Release。

### 升级注意事项

- 本次无破坏性变更，但建议先在后台确认短链功能开关、创建口令和推荐内容配置，再对外开放创建权限。

## Unreleased - feat/video-generation

### 重点更新

- 新增**文生视频**和**图生视频**两种工作台任务模式，覆盖 OpenAI Sora、Google Veo、Runway Gen 4.x、Luma Ray、MiniMax Hailuo、快手可灵、字节 Seedance、阿里 Wan、腾讯混元/Yt-Video、fal.ai Happy Horse 共 10 家官方供应商的 12 个协议端点（详见 `docs/requirements/VIDEO_GENERATION_REQUIREMENTS.md`）。
- 统一供应商模型目录扩展支持 10 个新 `ProviderKind`、12 个新 `ProviderProtocol`、5 个进阶视频 `ModelTaskCapability`，并新增 `VideoModelFeatures` 与 `VideoModelDefaults` 用于按模型描述视频能力与默认参数。
- 工作台提示词工具栏新增「文生视频」「图生视频」入口按钮，自动根据是否有源图片切换可用状态；提交在视频模式下会进入新的异步任务管道（submit → poll → download）。
- 视频结果使用全新 `VideoOutput` 面板呈现，支持排队、生成中、已完成、失败、远端 URL 已失效等状态及对应操作（取消、下载、复制提示词、复制诊断信息）。

### 架构

- `src/lib/provider-model-catalog.ts`：扩展 `ProviderKind`、`ProviderProtocol`、`ModelTaskCapability`、`ModelCapabilities.features.video`、`ModelTaskDefaults.video`，并补充 10 个视频模型名推断分支。
- `src/lib/video-types.ts`：新增视频生命周期、参数、源资产、结果资产、历史元数据、同步配置等类型与归一化器。
- `src/lib/video-history.ts`：视频历史 `localStorage` 读写、归一化、合并。
- `src/lib/video-job-store.ts`、`src/lib/video-asset-store.ts`：基于 IndexedDB 的可恢复任务状态和视频/缩略图/分镜表资产持久化。
- `src/lib/db.ts`：Dexie v5 迁移新增 `videoBlobs` 与 `videoJobs` 两张表，索引保持极简以避免移动端 Chrome 在升级时扫描大 blob。
- `src/lib/sync/manifest.ts`：`ManifestImageEntry.role` 扩展 `video-output | video-thumbnail | video-spritesheet | video-source`；`historyType` 扩展 `'video'`；`SnapshotManifest.videoHistory` 字段。
- `src/lib/sync/provider-config.ts`：`SyncAutoSyncScopes` 新增 `videoHistory | videoSourceImages | videoThumbnails | videoFiles`，其中视频文件默认关闭以避免带宽与对象存储成本失控。
- `src/lib/video-executor.ts`：proxy / direct / desktop 三态调度的执行器，含轮询退避（默认 5s → 30s）、abort 信号传播、Tauri 命令降级到 Web 路由。
- `src/app/api/video/{create,poll,download,cancel}/route.ts`：四条 Next.js API 路由，复用密码哈希校验、`validatePublicHttpBaseUrl` 安全策略、`getClientDirectLinkRestriction` 直连策略，并以流式响应转发视频下载。
- `src-tauri/src/proxy/video.rs` 与 `src-tauri/src/proxy/commands.rs`：四个 Tauri Rust 命令骨架，已注册到 `lib.rs`，目前对 Phase D 待补适配器返回明确的「实现待补」提示，复用 `validate_public_http_base_url` 强制公网 URL 安全策略。
- `src/lib/video-providers/`：`adapter.ts`（合约）、`registry.ts`、`bootstrap.ts` 自启动注册器、`openai-videos.ts`（Sora 完整实现）、`dashscope-video-generation.ts`（Wan 完整实现）、`placeholder-adapters.ts`（其余 10 个协议的结构化占位实现）。
- `src/hooks/useVideoTaskManager.ts`：React Hook 把执行器、任务存储、历史模块组合成 UI 可订阅的任务流，支持自动下载、abort、刷新可恢复任务。
- `src/components/video-output.tsx`：视频输出面板组件，覆盖空、进行中、成功、失败、过期、取消所有状态，严格使用语义化主题表面、Tooltip、i18n。
- `src/components/editing-form.tsx`：工具栏新增视频按钮、视频模式守卫、模式间切换辅助函数。
- `src/lib/i18n/messages.ts`：新增 171 个 `video.*` / `settings.video.*` i18n key，简体中文与英文同步维护。
- `src/lib/url-params.ts`：分享链接参数解析新增 `videoTaskMode | videoCatalogEntryId | videoRawModelId` 三个字段。

### 已知限制

- 12 个视频供应商中目前完整接入 OpenAI Sora 与阿里 Wan 两个参考协议；其余 10 个协议的具体实现作为占位适配器在调用时返回明确的「Phase D 实现待补」提示，UI 与同步层已支持，可按需补完。
- 视频任务的端到端提交按钮在工作台首屏暂以提示信息形式承载（`video.error.notConfigured`）；待后续将默认视频模型选择落地到设置页后再贯通完整闭环。
- 桌面端 `proxy_video_*` Rust 命令完成签名层骨架，等待与 TS 侧适配器同步补齐协议。

## v2.11.2 - 2026-05-20

### 重点更新

- 优化提示词输入框输入性能：降低分享、提示词模板和批量规划弹窗在输入过程中的同步重渲染压力，长提示词编辑更顺滑。
- 分享链接携带 API Base URL 和 API Key 时改为按供应商端点合并：不同 Base URL 自动新增端点并选中，同 Base URL 只更新该端点的 API Key，不再覆盖默认配置。
- 任务追踪区在失败任务存在时收紧批量操作展示：清理按钮文案改为“清空”，移动端批量重试/清空仅保留图标，并隐藏后续空并发槽位。
- 批量规划草稿恢复后会立即隐藏恢复提示，提示词模板入口图标也改为和批量入口区分，减少误点和误解。

### 稳定性与错误处理

- 分享配置持久化补充 provider instance 级测试覆盖，验证同端点更新、不同端点新增、显式实例 ID 复用、API Key-only 分享不创建空端点等场景。
- 保持官方默认端点的旧字段兼容写入，同时让第三方中转端点走 providerInstances，兼容旧配置读取与新端点并存。

### 文档与发布

- 更新分享与云同步文档，说明分享接收方保存 API 配置时的端点合并规则。
- 同步更新批量任务和交互优化需求文档里的“清空”按钮文案说明。
- 同步更新 Web、package-lock、Tauri 和 Cargo 版本号到 `2.11.2`，用于触发 `v2.11.2` GitHub Release、桌面端构建和 Android APK 构建。

### 升级注意事项

- 本次无破坏性变更。发布后建议重点验证：长提示词连续输入、多条不同端点分享链接保存、同端点不同 API Key 更新、失败任务批量重试/清空，以及移动端任务栏一行展示。

## v2.11.1 - 2026-05-20

### 重点更新

- 批量规划预览更稳：当模型输出的 `tasks` 数组缺少逗号、被截断或夹杂多余字符时，客户端会尽量保留已完整返回的任务，而不是整批直接失败。
- 批量任务追踪区新增失败项批量操作，支持一键重试全部失败任务，以及清空失败/已完成任务，失败行同时显示执行耗时。
- 网页剪贴板粘贴现在能同时识别图片和文本，图片进入源图区，文本进入提示词区，避免混合内容被错误路由。
- 批量规划对话框的数量策略更清晰：固定数量只显示目标任务数，AI 自动只显示最大任务数，并补充了说明提示。

### 稳定性与错误处理

- 批量规划的 Web API、Tauri 代理和本地解析逻辑统一提升到更高的输出上限，降低大批量规划时的 JSON 解析失败率。
- 批量规划草稿与预览回填逻辑更保守，避免在长输入和大任务数场景下丢失已保存的规划状态。
- 任务错误卡片继续保留原始错误与重试语义，失败任务的执行时长现在可直接看到，便于判断是短失败还是长时间执行后失败。

### 文档与发布

- 同步更新批量生图需求文档与交互优化需求文档，补充批量重试、数量策略收口和剪贴板混合内容处理说明。
- 同步更新 Web、package-lock、Tauri 和 Cargo 版本号到 `2.11.1`，用于触发 `v2.11.1` GitHub Release、桌面端构建和 Android APK 构建。

### 升级注意事项

- 本次无破坏性变更。发布后建议重点验证：大于 10 条的批量规划预览、混合图片+文本的网页粘贴、失败任务一键重试/清空，以及 Web API route 与 Tauri 代理链路是否一致。

## v2.11.0 - 2026-05-20

### 重点更新

- 新增批量生图规划工作流：支持把长文本、创作需求或参考图组合规划为多条可编辑任务预览，由 AI 自动判断内容拆分或多版本探索，确认后再批量进入现有任务队列。
- 批量规划同步覆盖 Web API route 与 Tauri 桌面代理，带源图片的批量任务会继承当前参考图并沿用现有图片编辑、任务追踪、历史与同步链路。
- 工作台 UI 升级为语义主题 token 与 `WorkbenchCard`、`IconButton`、`Popover` 等基础组件，主面板、弹窗、管理后台移动端和空状态布局更统一，并修复顶部渐变与空结果居中显示。
- 交互优化 Phase A/B 落地：任务错误分类与重试提示、多页签完成提醒、配置导入导出、API Key 连接测试、分享链接长度分级、历史清空撤销、跟随系统主题、轮播暂停/播放和离线横条等体验补齐。
- 历史面板改为单卡 blob 订阅、`IntersectionObserver` 懒加载与列表虚拟化，降低大历史列表滚动、预览加载和资源订阅的性能压力。

### 稳定性与错误处理

- 修复仅返回 partial image stream 的生成结果处理，避免只返回 partial 数据时被误判为失败或丢失结果。
- 增强任务错误分类、`Retry-After` 倒计时、认证/额度类不可重试提示和原始错误复制能力，让失败任务更容易判断和恢复。
- 增加 localStorage 配额预警、JSON 配置错误行列定位、SSR/hydration 守护、reduced-motion 全局兜底和软键盘安全区避让基础设施。
- 补齐 Zoom Viewer 焦点陷阱、跳到主内容、状态颜色叠加图标、图标按钮 `aria-label`、Tauri 外链打开封装和断图占位提示，提升可访问性与桌面端一致性。

### 文档与发布

- 新增批量生图需求文档，并更新 UI 升级、交互优化、云同步协议扩展、视频生成等需求文档和索引。
- 新增或扩展 `batch-plan`、`api-error-category`、`provider-connection-test`、`config-export`、`notice-persistence`、`task-eta`、`taskExecutor` 等测试覆盖。
- 同步更新 Web、package-lock、Tauri 和 Cargo 版本号到 `2.11.0`，用于触发 `v2.11.0` GitHub Release、桌面端构建和 Android APK 构建。

### 升级注意事项

- 本次无破坏性变更。发布后建议重点验证批量规划预览与批量入队、带源图片批量编辑、历史列表滚动性能、错误任务重试提示，以及 Web API route 与 Tauri 代理链路。
- 配置导出新增“含密钥”选项，导出的 JSON 会包含敏感凭据，生产环境使用时需按团队密钥管理规范保存和传递。

## v2.10.3 - 2026-05-17

### 重点更新

- 管理后台登录表单和管理页面密码输入框新增可见性切换按钮，支持切换明文/密文显示。
- 管理后台 API 修复代理环境下的来源校验逻辑，支持通过 `X-Forwarded-Host`、`X-Forwarded-Proto` 和 `Forwarded` 请求头识别真实来源。

### 稳定性与错误处理

- 修复代理/反向代理部署时管理后台因 `Host` 头被改写导致合法请求被误拦截的问题。
- 密码可见性切换组件复用性好，管理后台多处密码输入场景统一体验。

### 文档与发布

- 同步更新 Web、package-lock、Tauri 和 Cargo 版本号到 `2.10.3`，用于触发 `v2.10.3` GitHub Release、桌面端构建和 Android APK 构建。

### 升级注意事项

- 本次无破坏性变更。如使用 Nginx/Caddy 等反向代理部署管理后台，建议验证代理头传递后登录状态是否正常。

## v2.10.2 - 2026-05-17

### 重点更新

- 生成任务支持一键重试，失败任务重试时恢复提交时的提示词、源图片和参数快照，不会读取当前已改动的输入内容。
- 自定义分辨率输入新增尺寸推荐能力，输入尺寸不符合模型限制时，界面会推荐可用的接近分辨率并支持一键套用。

### 稳定性与错误处理

- 优化任务管理器内部状态流转，降低失败/重试/并发任务间的状态不一致风险。
- 失败任务关闭或清理后自动释放对应快照占用，任务输出面板与任务追踪显示更准确。
- 补全相关中英文翻译文案，确保任务重试、自定义尺寸推荐等新增 UI 在多语言界面下一致。

### 文档与发布

- 更新《工作台界面说明》文档，补充自定义分辨率推荐和失败任务重试行为说明。
- 同步更新 Web、package-lock、Tauri 和 Cargo 版本号到 `2.10.2`，用于触发 `v2.10.2` GitHub Release、桌面端构建和 Android APK 构建。

### 升级注意事项

- 本次无破坏性变更。发布后建议重点验证失败任务重试是否恢复原始参数、自定义尺寸推荐的推荐值是否合理、文档与实际界面一致。

## v2.10.1 - 2026-05-17

### 重点更新

- 自定义模型管理新增「刷新模型」与「从已读取模型列表选择」交互，图片生成端点和图生文端点都可以拉取远端模型列表后直接勾选添加，免去手工复制模型 ID。
- 图生文模式按钮在没有源图时改为快速切换回文生图，无需先添加源图也能在两种模式间来回切换。
- 图生文历史和审计记录的批量删除和清空对话框完善，删除前会有明确的二次确认，操作语义更清晰。

### 稳定性与错误处理

- 修复默认供应商实例丢失旧版 API Key 与 Base URL 凭据的问题，老用户升级后无需重新填写默认 OpenAI / Gemini / SenseNova / Seedream 的密钥。
- 修复图生文按钮在切换模式时的禁用与提示态错误，按钮状态、aria 属性和 tooltip 与实际可用模式保持一致。
- 完善图生文历史选中态在异步删除失败时的处理，删除被拒绝时不会错误地清空选中集合或退出多选模式。
- 图生文历史卡片标签和「+N」「源图待恢复」等覆盖层适配亮色主题，避免低对比度的纯黑底色块。

### 文档与发布

- 在《供应商与系统设置》文档中补充「刷新模型 + 从已读取模型列表选择 + 添加所选」流程，以及图生文与多模态独立端点的说明。
- 在 `AGENTS.md` 中补充国际化文案、禁止使用浏览器原生模态控件、文档同步更新等开发约束。
- 补全图生文历史、审计记录批量操作相关的中英文案桥接，多语言界面下的二次确认对话框文案保持一致。
- 同步更新 Web、package-lock、Tauri 和 Cargo 版本号到 `2.10.1`，用于触发 `v2.10.1` GitHub Release、桌面端构建和 Android APK 构建。

### 升级注意事项

- 本次无破坏性变更。发布后建议重点验证：默认供应商凭据是否被正确恢复、模型刷新与「添加所选」按钮、图生文模式按钮在无源图状态下的切换、图生文与审计记录的二次确认对话框文案在亮/暗主题与中/英文下的显示。

## v2.10.0 - 2026-05-16

### 重点更新

- 新增应用语言与旧界面文案桥接能力，系统设置、生成表单、历史面板、任务状态和提示词模板等核心界面具备更完整的多语言切换基础。
- 新增统一供应商模型目录和模型发现能力，支持把图片生成、图生文与提示词润色模型按供应商、端点、任务能力和默认模型统一管理。
- 图生文历史体验升级，新增独立历史列表、详情查看器、源图资产读取和结果恢复能力，便于回看、复制和继续使用图片理解结果。

### 稳定性与错误处理

- 修复移动端和 Tauri 客户端手势返回时弹窗无法先关闭的问题，系统设置、分享、确认框、提示词模板、大图预览等弹层现在会按栈顺序优先关闭，避免直接返回页面或退出 App 界面。
- 限制提示词输入框高度，长提示词编辑时页面布局更稳定。
- 同步补齐 Tauri 侧模型发现代理和历史资产读取链路，桌面静态包不依赖 Next.js API route 也能使用对应能力。

### 文档与发布

- 新增国际化、统一模型目录和图生文历史相关需求/实施文档，方便后续继续拆分和验收。
- 同步修复 Tauri Release workflow action 配置，降低 tag 发布构建失败概率。
- 同步更新 Web、package-lock、Tauri 和 Cargo 版本号到 `2.10.0`，用于触发 `v2.10.0` GitHub Release、桌面端构建和 Android APK 构建。

### 升级注意事项

- 本次无破坏性变更。发布后建议重点验证语言切换、模型目录发现、图生文历史恢复、移动端手势返回关闭弹窗、Web API route 与 Tauri 代理链路。

## v2.9.0 - 2026-05-15

### 重点更新

- 新增图生文/图片理解模式，上传源图后可切换生成文本，支持提示词反推、图片描述、设计规范、OCR 与自由问答。
- 新增独立多模态供应商与模型配置，不污染图片生成模型列表，支持 OpenAI `gpt-5.5`/`gpt-5.4` 与 OpenAI-compatible 端点。
- 文本结果面板支持流式输出、结构化字段解析、复制和回填到生成器，方便把图片理解结果继续用于图片生成。

### 稳定性与错误处理

- Web 端新增 `/api/image-to-text` 独立路由，并保留密码校验、图片大小限制和严格 URL 安全检查。
- Tauri 新增图生文代理和流式命令，桌面静态包不依赖 Next.js API route 也能使用多模态文本生成。
- 修复源图缩放加载与本地 blob 恢复相关问题，降低历史源图预览一直加载或无法回读的概率。

### 文档与发布

- 新增 agent development guide，并同步项目级 Agent 指令，方便后续跨 Web、Tauri 与移动端协作开发。
- 同步更新 Web、package-lock、Tauri 和 Cargo 版本号到 `2.9.0`，用于触发 `v2.9.0` GitHub Release、桌面端构建和 Android APK 构建。

### 升级注意事项

- 图生文需要配置可用的多模态 API Key 和 Base URL；OpenAI-compatible 端点需按实际能力选择 Responses API 或 Chat Completions。
- 发布后建议重点验证图生文流式输出、结构化输出、发送到生成器、Web API route 与 Tauri 代理链路。

## v2.8.2 - 2026-05-14

### 重点更新

- 支持把网页里复制的图片直接粘贴成源图片，不再只落成 `img` 标签 HTML 文本。
- 提示词输入框、页面空白区域以及本地复制的图片文件，现在都可以直接识别并添加到源图片里。

### 稳定性与错误处理

- 修复图片粘贴后重复出现在源图片区中的问题，避免同一张图被插入两次。
- 修复页面非输入框区域粘贴时只拿到 `img` HTML 文本的问题，图片内容现在会优先路由到源图片处理。

### 文档与发布

- 整理并迁移规划类文档到 `docs/requirements/`，同步更新 README 链接。
- 同步更新 Web、package-lock、Tauri 和 Cargo 版本号到 `2.8.2`，用于触发 `v2.8.2` GitHub Release、桌面端构建和 Android APK 构建。

### 升级注意事项

- 本次主要是剪贴板图片粘贴链路和文档结构调整，升级后建议重点验证网页复制图片、本地文件复制图片，以及输入框外页面区域的粘贴行为。

## v2.8.1 - 2026-05-14

### 重点更新

- 高级选项中的 Google Gemini、Seedream 和 SenseNova 图片尺寸改为按清晰度、比例和分辨率分组展示，和 OpenAI 尺寸选择保持一致。
- Gemini 尺寸表按官方图片生成文档补齐 `512`、`1K`、`2K`、`4K` 档位与 14 种比例，并在请求中发送官方 `responseFormat.image.aspectRatio/imageSize` 配置。
- 清空提示词按钮现在会同时清空源图片、预览、蒙版和缩放状态，减少编辑模式残留内容。
- 重新生成 Android launcher、round 和 foreground 图标资源，修正 Tauri Android APK 图标显示不完整的问题。

### 稳定性与错误处理

- 后台 SQLite 数据库路径支持 `~/` 展开，生产和本地默认路径从 `/tmp` 调整到 `~/work/gpt-image-playground/`，降低临时目录清理导致后台数据丢失的风险。
- 129 服务器部署改为使用本地或远端 Docker 构建运行包，避免小内存服务器上重建 native 依赖和 Next.js 产物导致发布不稳定。
- 生产部署脚本补齐 `.env.production` 校验、远端环境备份、构建产物同步和缓存头检查，降低生产配置漂移风险。

### 文档与发布

- 同步更新 Web、package-lock、Tauri 和 Cargo 版本号到 `2.8.1`，用于触发 `v2.8.1` GitHub Release、桌面端构建和 Android APK 构建。
- 更新后台数据库路径示例和部署文档，说明 `~/work/gpt-image-playground/` 持久路径用法。

### 升级注意事项

- 如果服务器上已有 `/tmp/gpt-image-playground/promo-admin.sqlite` 数据，需要在发布前迁移到 `~/work/gpt-image-playground/promo-admin.sqlite` 对应路径，避免后台配置看起来被重置。
- 发布后建议重点验证 Gemini 尺寸请求、源图清空行为、Android APK 图标、管理后台数据库路径和两台服务器部署状态。

## v2.8.0 - 2026-05-13

### 重点更新

- 管理后台的展示位、展示组和展示素材列表体验得到优化，冗余列被移除，操作按钮更容易保持在视野内。
- 页面顶部和历史面板上的展示位改为圆角样式，生成区头部继续保持原有直角。

### 稳定性与错误处理

- 展示组停用和启用后的反馈改为右下角消息提示，支持自动消失和手动关闭。
- 桌面端读取线上展示内容的默认站点地址已经统一到生产域名，降低打包后读不到线上配置的风险。

### 文档与发布

- 清理用户可见文案中的展示措辞，统一改为中性描述，并同步更新使用文档、后台说明和发布说明。
- 同步更新 Web、package-lock、Tauri 和 Cargo 版本号到 `2.8.0`，用于触发 `v2.8.0` GitHub Release、桌面端构建和 Android APK 构建。

### 升级注意事项

- 这次发布包含管理后台与桌面端读取链路调整，发布前后都要确认后台页面、展示位接口和桌面端打包参数使用同一套线上站点配置。
- `NEXT_PUBLIC_*` 和 `AUTH_BASE_URL` 这类生产默认值改动后，需要重新构建或重启相关部署才能生效。

## v2.7.12 - 2026-05-12

### 重点更新

- Web 端新增可选的 DevTools 抑制开关，帮助分享页降低临时 API Key 被直接从浏览器开发者工具里拿走的概率。
- 分享页支持按环境变量单独开启该抑制逻辑，默认关闭，不影响桌面端使用。

### 稳定性与错误处理

- DevTools 抑制只作为额外摩擦层使用，不改变现有的请求逻辑和分享流程。
- 该能力只在 Web 应用中生效，桌面端 Tauri 运行方式保持不变。

### 文档与发布

- 新增 `NEXT_PUBLIC_DISABLE_DEVTOOL_SCOPE` 环境变量说明，并补充 Web 端、分享页和桌面端的适用范围。
- 同步更新 Web、package-lock、Tauri 和 Cargo 版本号到 `2.7.12`，用于触发 `v2.7.12` GitHub Release、桌面端构建和 Android APK 构建。

### 升级注意事项

- 这是一个降低误取风险的辅助功能，不是安全边界；真正的权限控制仍应由 Provider 侧的 Key 限额、权限和有效期负责。
- `NEXT_PUBLIC_*` 配置改动后需要重新构建或重启 Web 部署才会生效。

## v2.7.11 - 2026-05-12

### 重点更新

- 修复 Tauri 客户端文件系统存储模式下历史图片无法上传到云存储的问题，云同步现在会通过本地文件读取命令补取历史图片文件。
- 修复 Android APK 发布构建中应用图标仍回退到默认图标的问题，Android 工程初始化后会同步仓库内的 launcher icon 资源。
- 修复 Android 应用图标内容被二次缩放、显示不完整的问题，重新生成各密度 launcher、round 和 foreground 资源。
- 优化移动端默认示例图片右上角标签，改为浅色半透明背景并限制宽度，提升亮色和暗色主题下的可读性。
- 增强 Android Tauri 客户端粘贴分享链接的识别能力，在系统剪贴板事件未命中时，会对提示词输入框中的分享链接特征做低频兜底识别。
- 生成历史新增图片文件大小展示，小于 1MB 显示 KB，其余显示 MB，并统一保留两位小数。
- 分享 API Key 新增“只允许临时使用”选项，接收方会直接临时套用，不再弹出保存到本地的确认框，也不会写入配置。

### 稳定性与错误处理

- 云同步上传历史图片时会在 IndexedDB、浏览器可访问路径和 Tauri 本地文件系统之间依次兜底，降低本机路径、blob URL 失效导致的上传失败。
- Android Tauri 客户端的提示词兜底识别只在移动端 Tauri 环境启用，并且先按 `?` 或 URL 协议前缀过滤，避免普通提示词被反复解析。
- Android Release workflow 和本地 `build:android` 前置步骤都会同步 `src-tauri/icons/android` 到生成的 Android 工程，避免重新初始化工程后丢失自定义图标。

### 文档与发布

- 补充 Android 图标同步发布说明。
- 同步更新 Web、package-lock、Tauri 和 Cargo 版本号到 `2.7.11`，用于触发 `v2.7.11` GitHub Release、桌面端构建和 Android APK 构建。

### 升级注意事项

- 本次无破坏性变更。升级后建议重点验证 Tauri 文件系统历史图片云同步、Android APK 桌面图标、移动端示例历史标签和 Android 客户端分享链接粘贴识别。

## v2.7.10 - 2026-05-12

### 重点更新

- OpenAI 图片尺寸选择新增 1K、2K、3K、4K 与多种常用比例的分辨率预设，默认仍保持自动尺寸。
- 高级选项面板统一为分组 pill 选择样式，OpenAI、Google Gemini、Seedream 和 SenseNova 的参数布局更一致。
- Seedream 和 SenseNova 仍只展示各自支持的尺寸、响应格式、水印、序列生成等参数，避免混入不适用的 OpenAI 参数。

### 稳定性与错误处理

- 新增 OpenAI 分辨率预设合规性测试，确保内置尺寸满足 `gpt-image-2` 的边长、像素总量和比例限制。
- 保留自定义尺寸校验与供应商参数 JSON 兜底能力，降低 OpenAI 兼容端点新增参数时的配置成本。

### 文档与发布

- 同步更新 Web、package-lock、Tauri 和 Cargo 版本号到 `2.7.10`，用于触发 `v2.7.10` GitHub Release、桌面端构建和 Android APK 构建。

### 升级注意事项

- 本次无破坏性变更。升级后建议重点验证 OpenAI 4K 分辨率预设、Google Gemini 尺寸选项、Seedream 高级参数和 SenseNova 尺寸选择。

## v2.7.9 - 2026-05-11

### 重点更新

- 修复 macOS、Windows 和 Tauri 客户端生成历史中本地生成图片仍显示占位、点击大图预览一直加载的问题，本地图片现在会通过桌面端本地文件读取能力恢复并缓存显示。
- 分享链接新增来源标识，客户端粘贴普通或加密分享链接时会自动识别并应用参数，提升跨客户端分享流通能力。
- 重新生成并校正 Android APK 图标资源，修复 APK 应用图标与桌面端不一致的问题。

### 稳定性与错误处理

- 修复桌面端历史图片发送到编辑、下载和预览时对本地文件路径的读取兼容性，自定义图片存储目录也能正确回读历史图片。
- 补齐移动端和 Android 下系统配置、大图预览、分享、提示词模板库和高级选项等弹窗的安全区适配，避免系统状态栏遮挡关闭按钮。
- 优化生成历史费用展示，移动端默认显示两位小数并通过悬浮提示保留四位精度，同时修复单张图片费用出现两个 `$` 符号的问题。

### 文档与发布

- 同步更新 Web、package-lock、Tauri 和 Cargo 版本号到 `2.7.9`，用于触发 `v2.7.9` GitHub Release、桌面端构建和 Android APK 构建。

### 升级注意事项

- 本次无破坏性变更。升级后请重点验证桌面端本地生成历史、移动端弹窗关闭按钮以及 GitHub Release 中 Android APK 图标展示。

## v2.7.8 - 2026-05-11

### 重点更新

- 修复移动端恢复历史图片时的预览卡顿问题和恢复进度状态展示，图片恢复流程更加可靠。
- 优化 IndexedDB 存储策略，不再对图片 blob 建立索引并避免完整读取，恢复大体积历史时内存占用更低。
- 移除未使用的缩略图预加载逻辑，减少不必要的数据库读取和渲染开销。

### 稳定性与错误处理

- 修复移动端恢复预览时的死锁问题，确保恢复操作切后台回来后能正常继续。
- 修复正在同步中的历史记录状态标记，同步完成后能正确展示成功状态。
- 修复从 IndexedDB 恢复图片时缩略图刷新不生效的问题，恢复后能立即看到正确的预览图。
- 恢复并恢复后跳过本地图片检查时加入边界保护，避免异常状态下无限重试。

### 性能优化

- `handleSendToEdit` 改用 `useCallback` 包裹，提升组件 memoization 并减少不必要的重渲染。

### 升级注意事项

- 本次无破坏性变更。优化主要影响 IndexedDB 存储和恢复流程，升级后历史图片恢复速度和内存占用会有改善。

## v2.7.7 - 2026-05-11

### 重点更新

- 更新桌面端图标资源，重新生成了 Tauri、Android 和 iOS 的应用图标，发版包和安装入口的视觉识别更统一。
- 优化历史图片恢复时的同步检查逻辑，减少重复判断和无效读取，恢复流程更轻。
- 修复 Android 页面顶部安全区，避免状态栏遮挡内容。

### 稳定性与错误处理

- Android 签名和发布脚本补齐了幂等处理与 SDK 变量插值，降低重复执行发布流程时的构建失败概率。
- Android APK 构建不再加载桌面端专用的重启与更新权限，避免移动端打包因无效权限中断。
- macOS Release 会根据是否配置签名 Secrets 走不同构建分支，未签名产物会在 Release notes 中附带 Gatekeeper 打开说明，减少发布结果不明确的问题。
- 未配置 Apple 签名 Secrets 时，macOS workflow 不再向 Tauri 注入空签名证书变量，确保能正常产出未签名 DMG。

### 文档与发布

- 补充 Android 发行版签名和 APK 构建说明，明确 SDK、NDK、签名配置和产物路径。
- 同步更新 Web、package-lock、Tauri 和 Cargo 版本号到 `2.7.7`，用于触发 `v2.7.7` GitHub Release 与桌面端构建。

### 升级注意事项

- 本次主要影响桌面端资源和发布链路，发布后请重点确认 GitHub Release 里的 macOS、Windows、Linux 安装包以及 Android APK 是否都成功产出。

## v2.7.6 - 2026-05-11

### 重点更新

- 生成历史缩略图新增云同步状态感知：已同步图片显示绿色云朵，未同步图片显示可点击的上传云朵图标，可对单条历史记录单独补同步到云存储。
- 移动端恢复历史图片改为并发下载和批量写入 IndexedDB，降低手机 Chrome 从局域网对象存储恢复图片时的等待时间。
- 新增 Tauri Android APK 构建入口和 Android 专用包名配置，提供 `android:init`、`android:dev` 和 `build:android` 脚本。

### 稳定性与错误处理

- 图片上传、恢复和跳过远端已存在对象后，会同步更新历史记录与 IndexedDB 中的云同步状态，刷新页面后仍能正确展示。
- 同步和图片生成期间会尽量请求浏览器屏幕唤醒，并在移动浏览器切后台或无法保持唤醒时提示风险，减少锁屏、后台运行导致请求暂停或异常的误解。
- 云同步读取本地图片时支持从历史记录路径和服务端图片接口补取，提升文件系统模式历史图片的单条同步兼容性。

### 文档与发布

- 新增 Android APK 构建文档，说明 Android SDK/NDK、初始化、调试和 APK 输出路径。
- 同步更新 Web、package-lock、Tauri 和 Cargo 版本号到 `2.7.6`，用于触发 `v2.7.6` GitHub Release 与桌面端构建。

### 升级注意事项

- 本次无破坏性变更。Android APK 构建需要本机已配置 Android Studio、SDK、NDK、`JAVA_HOME` 和 `ANDROID_HOME`，首次构建前需执行 `npm run android:init`。
- 纯移动浏览器在锁屏或长时间后台运行时仍可能被系统暂停；本版本会尽量保持屏幕唤醒并提示风险，如需稳定后台能力建议使用原生壳或服务端任务队列。

## v2.7.5 - 2026-05-10

### 重点更新

- 提示词输入框的 `/` 模板搜索支持更多匹配结果和滚动加载，避免模板较多时只能看到前几项。
- 云存储同步默认不再要求远端删除权限；未开启远端删除时会继续正常同步新文件，并保留旧远端图片引用。

### 稳定性与错误处理

- 修复对象存储凭据缺少 `DeleteObject` 权限时，全量同步被批量删除保护拦截的问题；远端删除现在必须在云存储设置中显式开启后才会发布删除标记或请求删除对象。
- 修复部分真实移动端进入“提示词润色配置”后设置弹窗不能按移动端全屏布局自适应的问题，改为带 `vh` fallback 的弹窗内部分区滚动布局。

### 文档与发布

- 文档补充云存储推荐权限说明，明确普通同步只需要读取、列出和写入权限，删除权限为可选能力。
- 同步更新 Web、package-lock、Tauri 和 Cargo 版本号到 `2.7.5`，用于触发 `v2.7.5` GitHub Release 与桌面端构建。

### 升级注意事项

- 本次无破坏性变更。默认不会同步删除远端图片；如确实需要多设备同步删除，请确认对象存储凭据具备 `DeleteObject` 权限，并在云存储同步设置中开启“允许同步删除远端图片”。

## v2.7.4 - 2026-05-10

### 重点更新

- README 完全改写为面向用户的项目概览，并新增完整用户手册入口，帮助新用户从价值、场景和核心流程理解 GPT Image Playground。
- 新增系统化文档目录，覆盖快速开始、工作台、生成与编辑、提示词工作流、供应商设置、历史资产、分享同步、桌面端与部署等使用主题。
- 内置示例历史改为项目相关信息图，并将“一图速览”放在首位；空历史状态下用户可以直接通过示例图了解项目整体能力、提示词资产、多供应商、历史资产和分享同步。

### 稳定性与错误处理

- 内置示例元数据统一为 `gpt-image-2`、`high`、`webp`，避免示例图片、模型标签和输出格式不一致。
- 示例信息图不写入真实历史、IndexedDB 或云同步快照，继续保持与用户真实生成记录隔离。

### 文档与发布

- 文档补充截图和使用场景说明，降低从首次打开到完成生成、编辑、管理历史和配置同步的理解成本。
- 同步更新 Web、package-lock、Tauri 和 Cargo 版本号到 `2.7.4`，用于触发 `v2.7.4` GitHub Release 与桌面端构建。

### 升级注意事项

- 本次无破坏性变更。内置信息图只影响空历史状态下的示例展示，不会改变已有用户历史记录或同步数据。

## v2.7.3 - 2026-05-10

### 重点更新

- 生成历史在空列表时新增内置示例图片，可展示文生图、图像编辑、透明商品图和多图批量生成等典型效果；用户删除示例后会记住隐藏状态。
- 云存储自动同步支持按配置、提示词历史、提示词模板、图片历史和图片文件分范围同步，并可对最近生成图片做增量上传，减少不必要的全量同步。
- 删除单条、批量删除和清空历史时，只要已配置云存储，就会提供默认不勾选的“同时删除远端图片”确认项；未显式勾选时不会删除云端对象或远端清单记录。

### 稳定性与错误处理

- 云存储恢复改为非破坏性合并图片历史，恢复配置、恢复历史图片或分享链接自动恢复都不会用云端旧快照覆盖本地最新生成记录。
- 手动同步或恢复前会先刷新当前本地生成历史，避免刚生成的图片尚未落盘时被旧快照漏掉。
- 云存储清单新增修订号、设备 ID、父快照、清单备份和删除墓碑记录，并加入批量删除保护，降低误发布删除状态和恢复范围扩大的风险。
- 恢复范围、远端对象校验、元数据合并、自动同步范围和同步配置分享新增测试覆盖。

### 文档与发布

- README 和环境示例补充内置示例历史、云同步范围和 S3 服务器中转说明。
- 同步更新 Web、package-lock、Tauri 和 Cargo 版本号到 `2.7.3`，用于触发 `v2.7.3` GitHub Release 与桌面端构建。

### 升级注意事项

- 本次无破坏性变更。云存储远端图片删除必须在删除确认弹窗中单独勾选；未勾选时删除操作只影响本地历史和本地文件。

## v2.7.2 - 2026-05-10

### 重点更新

- 分享链接新增云存储同步配置选项，可在密码加密链接中带上 S3 Endpoint、Bucket、凭据、远端路径和恢复策略，方便接收者保存后继续恢复配置、历史记录或历史图片。
- 接收分享链接时新增云存储同步配置确认流程，支持仅保存配置，或按分享方设置恢复配置、历史记录、最近图片或全部历史图片。

### 稳定性与错误处理

- 云存储分享默认要求密码加密，并在复制前要求确认凭据风险；全量图片恢复会额外提示耗时、带宽和本地空间风险。
- 修复浅色主题下提示词输入框文字和占位符颜色过浅的问题，确保输入内容可读。
- 新增同步配置分享、加密分享参数和 URL 解析测试覆盖，降低分享恢复策略丢失或解析失败风险。

### 文档与发布

- 同步更新 Web、package-lock、Tauri 和 Cargo 版本号到 `2.7.2`，用于触发 `v2.7.2` GitHub Release 与桌面端构建。

### 升级注意事项

- 本次无破坏性变更。云存储同步配置分享会包含对象存储访问凭据，请只通过密码加密链接发送给可信接收者，并按需开启自动恢复或全量图片恢复。

## v2.7.1 - 2026-05-10

### 重点更新

- 修复新设备配置云存储时被直连 API Key 校验阻断的问题；系统设置现在会保存用户填写的云存储配置，并用 Web 通知提醒直连生成所需的供应商 API 配置。
- “恢复历史图片”会同步恢复可显示的历史记录元数据，并将远端图片记录规范为 IndexedDB 本地图片，恢复完成后无需刷新即可在生成历史中看到对应图片。

### 稳定性与错误处理

- 移除生产代码中的 `alert` / `confirm` 系统弹窗，改用应用内通知和 Web UI 确认弹窗，避免 Tauri 客户端因原生弹窗不可用导致功能失效。
- 桌面端图片自定义存储目录改为 Web 输入框填写，移除 Tauri dialog 插件和权限依赖，目录选择能力不再依赖原生系统控件。
- 新增云恢复历史记录规范化测试，覆盖远端文件系统路径恢复到 IndexedDB 后的前端显示行为。

### 文档与发布

- 同步更新 Web、package-lock、Tauri 和 Cargo 版本号到 `2.7.1`，用于触发 `v2.7.1` GitHub Release 与桌面端构建。

### 升级注意事项

- 本次无破坏性变更。桌面端如使用自定义图片目录，请在系统设置中直接填写本机文件夹绝对路径。

## v2.7.0 - 2026-05-09

### 重点更新

- 新增 S3 兼容对象存储云同步，可在系统设置中为当前浏览器或桌面端配置 Endpoint、Bucket、Access Key、Secret Key、命名空间和请求方式。
- 生成历史面板新增云同步入口，支持分别同步配置与记录、同步历史图片、恢复配置与记录、恢复历史图片，并可按最近小时/天范围处理图片。
- 历史图片同步使用稳定内容寻址对象 Key，并通过远端对象大小与 `sha256` 元数据跳过已存在图片，减少重复上传和下载。
- Web 端默认支持客户端直连对象存储，Tauri 桌面端通过 Rust 网络层执行 S3 请求，可绕开浏览器 CORS 限制；同时保留可选服务器中转 fallback。

### 稳定性与错误处理

- 新增同步快照、配置脱敏、对象 Key 校验、S3 客户端、API 密码校验和连接测试等测试覆盖，降低云同步误写、误删和凭据泄露风险。
- 同步/恢复过程会展示目标 bucket/前缀、manifest key、快照时间、开始时间、耗时、图片总数、完成数、失败数与跳过数，方便确认远端状态。
- 恢复流程支持将配置与图片分开处理，避免恢复历史图片时覆盖当前本地配置、提示词或历史记录。

### 文档与发布

- README 与 `.env.example` 新增 S3/RustFS/MinIO 配置、CORS 要求、客户端直连和服务器中转说明。
- 同步更新 Web、package-lock、Tauri 和 Cargo 版本号到 `2.7.0`，用于触发 `v2.7.0` GitHub Release 与桌面端构建。

### 升级注意事项

- 本次无破坏性变更。S3 云同步为可选功能，升级后默认不启用；使用前需要在系统设置中填写对象存储配置。
- Web 端客户端直连要求对象存储正确配置 CORS，至少允许 `GET` / `PUT` / `HEAD` / `OPTIONS` 并暴露 `ETag` 与 `x-amz-meta-sha256`。
- 云同步快照不会保存 S3 凭据，也会对应用配置中的图像供应商 API Key 做脱敏处理；恢复后仍需在当前设备重新配置相关密钥。

## v2.6.4 - 2026-05-09

### 重点更新

- 供应商 API 配置支持同一供应商类型保存多个命名端点，新增端点可按域名自动命名，并可在高级选项中直接切换具体供应商实例。
- 命名供应商端点支持独立选择可用模型并添加该端点提供的自定义模型 ID，原“自定义模型 ID”入口整合为供应商实例下的模型能力配置。
- OpenAI 兼容端点填写更宽容：可只输入域名或带斜杠域名，请求时会自动按 OpenAI 兼容规范补齐 `/v1`，已有 `/v1` 或自定义路径则保持兼容处理。
- 提示词润色新增 8 种内置预设：均衡润色、精简润色、编辑精修、电影质感、照片写实、插画艺术、品牌视觉和极简设计，覆盖通用优化与风格生成两类常用场景。
- 系统设置新增独立“提示词润色”配置页面，可组合内置预设与自定义润色提示词，并调整润色按钮下拉菜单中的显示顺序。
- 润色参数新增 Thinking 开关、effort 级别和 format 格式支持，适配不同模型的深度推理配置。

### 分享与兼容性

- 分享链接支持携带命名供应商端点 ID，普通分享与密码加密分享都会保留当前端点选择；API Key 和 Base URL 分享流程继续保留风险确认与接收者保存选择。
- URL 参数新增 `providerInstance` / `providerInstanceId` / `instance`，用于恢复高级选项中的命名供应商端点选择。
- 旧版单供应商 API Key/Base URL 配置会自动迁移为每个供应商类型的默认端点，避免升级后丢失既有配置。

### 稳定性与错误处理

- 高级选项弹窗添加屏幕阅读器描述，提升无障碍访问体验。
- 新增供应商实例、分享参数、加密分享、共享配置、OpenAI 兼容 Base URL 归一化和提示词润色配置测试覆盖。

### 文档与发布

- README 更新供应商端点配置说明，记录命名端点、模型选择和自定义模型整合流程。
- 同步更新 Web、package-lock、Tauri 和 Cargo 版本号到 `2.6.4`，用于触发 `v2.6.4` GitHub Release 与桌面端构建。

### 升级注意事项

- 本次无破坏性变更。旧版单供应商 API Key/Base URL 配置会在首次加载时自动迁移为默认端点，无需手动干预。

## v2.6.3 - 2026-05-08

### 重点更新

- 桌面端外部链接改由 Tauri opener 原生打开，关于弹窗、设置和历史等位置的跳转不再被内置 WebView 行为限制。
- 加密分享链接支持携带解密密码自动解锁，接收者打开完整链接后可直接恢复分享内容，并减少手动输入密码的步骤。
- 首次访问时默认跟随系统亮色/暗色偏好，只有系统主题不可判定时才回退到亮色主题。

### 稳定性与错误处理

- 分享链接中的 API Key、Base URL 和模型已与本地保存配置一致时，不再重复弹出保存共享配置提示。
- 分享复制优先使用桌面端原生剪贴板，浏览器环境继续使用 Web Clipboard 和兜底复制逻辑，提升跨运行环境复制稳定性。
- 新增分享配置和主题初始化测试覆盖，确保自动解锁、配置提示跳过和系统主题回退逻辑可回归验证。

### 文档与发布

- 新增网络存储同步策略研究文档，记录 IndexedDB、远程存储与多端同步后续方案分析。
- 同步更新 Web、package-lock、Tauri 和 Cargo 版本号到 `2.6.3`，用于触发 `v2.6.3` GitHub Release 与桌面端构建。

### 升级注意事项

- 本次无破坏性变更。桌面端本地构建需包含新增的 Tauri opener/clipboard 依赖与权限配置，以确保外部链接打开和剪贴板复制能力可用。

## v2.6.2 - 2026-05-07

### 重点更新

- 修复 Tauri 桌面端提示词模板库未加载内置预设的问题，桌面包现在无需依赖 Web API 也能展示全部默认模板。
- 桌面端图片存储模式选择“文件系统”时会真正写入本地文件，并支持在系统设置中选择自定义保存目录；未选择时使用应用默认数据目录。
- 加密分享体验优化：复制加密链接优先使用桌面原生剪贴板，随机生成解密密码后会自动显示，并可分别切换两处密码输入框的可见性。

### 稳定性与错误处理

- 本地图片保存、读取、删除和历史记录预览统一支持 Tauri 本地文件路径，避免文件系统模式回退到 IndexedDB 或无法渲染缩略图。
- 自定义图片保存目录要求使用绝对路径，减少路径歧义并避免异常写入位置。
- 保持 Web 端提示词模板、分享复制和图片存储逻辑不变，桌面端专属能力通过 Tauri IPC 与插件权限隔离接入。

### 文档与发布

- 同步更新 Web、package-lock、Tauri 和 Cargo 版本号到 `2.6.2`，用于触发 `v2.6.2` GitHub Release 与桌面端构建。

### 升级注意事项

- 本次无破坏性变更。桌面端如选择文件系统存储并配置自定义目录，请确认目录为本机绝对路径且当前用户具备写入权限。

## v2.6.1 - 2026-05-07

### 重点更新

- 系统设置中“供应商 API 配置”改为独立内页入口，点击后进入专属配置页面，各供应商 API Key 和 Base URL 分别展开配置，不再挤在一个大折叠块里。
- 关于弹窗精简，移除面向开发者的口语化描述，保留应用信息与更新检查。
- 桌面端专属引导：Web 运行时自动隐藏桌面代理/调试等配置项，改用统一提示引导用户下载桌面端；配置跨域或直连请求失败时同样给出明确跳转引导。

### 交互体验

- 对话框移动端自适应高宽：小屏幕下弹窗自动撑满视窗并保留安全区域底部间距，关闭按钮扩大至 44×44 触摸热区，彻底解决移动端上下内容被遮挡无法操作的问题。
- 系统设置取消操作增加未保存确认，避免误点直接流失已填写的 API 配置。

### 稳定性与错误处理

- 修复提示词润色旧配置隐式覆盖内置预设的问题，现在只有用户手动选中“已保存自定义”或“临时自定义”才会覆盖。
- 共享桌面端引导文案模块，统一连接策略、任务执行器、提示词润色等模块中的错误提示语，消除分散维护导致的文案不一致。
- 增加桌面引导、连接策略和润色配置相关测试覆盖。

### 文档与发布

- 同步更新 Web、package-lock、Tauri 和 Cargo 版本号到 `2.6.1`，用于触发 `v2.6.1` GitHub Release 与桌面端构建。

### 升级注意事项

- 本次为功能改进版本，无需额外迁移操作。系统设置中的历史配置会自动保留，供应商 API 配置页面进入路径略有变化但不影响功能使用。

## v2.6.0 - 2026-05-06

### 重点更新

- 提示词润色升级为多预设选择体验，内置均衡、简洁、编辑优化、电影感、写实摄影、插画、商业产品和极简构图等多种场景化润色规则。
- 润色按钮现在会像历史和模板一样弹出选择框，支持搜索内置预设、按源图自动推荐编辑优化预设，并可输入仅本次生效的临时自定义润色提示词。
- 系统设置中的润色配置重新梳理为“内置预设”“已保存自定义”“临时自定义”三种独立来源：内置预设只读可查看，已保存自定义可编辑并在下拉中手动选择，临时自定义不会覆盖系统设置。
- 桌面端新增更新检查、应用内通知和更多代理配置控制，Rust/Tauri 代理能力继续扩展到提示词润色、Gemini/OpenAI 配置和桌面直连辅助场景。

### 交互体验

- 修复亮色主题下润色选择框、历史下拉、模板快捷命令和分享按钮 hover 状态的文字/图标对比度问题，浅色界面下更容易阅读。
- 统一提示词区域下拉层关闭行为：点击输入框、空白区域或切换选择后，润色、历史和模板下拉都会按预期关闭，减少残留浮层干扰。
- 设置面板会展示当前内置润色预设的完整系统提示词、分类和只读状态，用户能清楚知道每个预设如何工作。
- 桌面端全屏预览补充缩放手势修复，历史记录相关操作在桌面环境中更稳定。

### 稳定性与错误处理

- 提示词润色请求不再让已保存自定义提示词隐式覆盖选中的内置预设，只有用户在下拉中明确选择“已保存自定义”或“临时自定义”时才会作为本次请求覆盖。
- 增加提示词润色预设、系统提示词解析、桌面配置和代理命令相关测试，覆盖旧配置默认值、大小写空格兼容和桌面代理边界。
- 桌面导出构建保留必要 API Routes，避免桌面构建过程误删服务端接口导致运行时异常。

### 文档与发布

- 同步更新 Web、package-lock、Tauri 和 Cargo 版本号到 `2.6.0`，用于触发 `v2.6.0` GitHub Release 与桌面端构建。

### 升级注意事项

- 如此前在系统设置里填写过自定义润色提示词，升级后它会作为“已保存自定义”选项出现在润色下拉中，但不会再自动覆盖内置预设；需要使用时请在下拉中手动选择。

## v2.5.0 - 2026-05-06

### 重点更新

- 桌面端服务器中转模式接入 Rust 请求代理，图片生成、编辑和远程图片代理不再依赖 Node 服务端，降低 Tauri 安装包体积压力。
- 桌面端历史图片、远程图片发送到编辑和本地图片下载改由 Tauri IPC 调用 Rust 处理，保持 Web 端 Next API Routes 行为不变。
- OpenAI 桌面端流式预览改为通过 Tauri Channel 转发图片生成事件，支持 partial image progress 与完成事件回传。

### 稳定性与错误处理

- 增加远程图片代理的 URL、重定向、DNS 私网地址和响应体大小校验，降低 SSRF 与异常大文件风险。
- 增加桌面端上传文件大小限制、本地图片文件名校验和删除路径校验，避免路径穿越和误删非生成图片。
- 补充 Rust SSE 解析、远程/本地图片处理、OpenAI 流式代理和桌面 IPC 包装测试，覆盖畸形事件、`[DONE]` sentinel、上传大小限制与 Channel 消息转发。

### 文档与发布

- 更新 Tauri Rust 请求代理阶段规划文档，记录 Phase 3/4 实现范围、验证结果、5 路审查结论和后续非阻塞跟进项。
- 同步 Web、package-lock、Tauri 和 Cargo 版本号到 `2.5.0`，用于触发 `v2.5.0` 桌面端 GitHub Release 构建。

### 升级注意事项

- 本次先部署到 `142` 测试环境验证 Docker 构建、GitHub Actions 桌面构建和人工使用体验；确认正常后再按需部署 `129` 生产环境。

## v2.4.1 - 2026-05-05

### 修复

- 修复 v2.4.0 精简关于弹窗时误删应用信息的问题，恢复展示版本号、作者、官网地址、GitHub 仓库和联系方式二维码。
- 关于弹窗元数据改为集中从 `package.json` 派生，并新增回归测试，确保后续发版时版本号、作者和项目链接不会再次丢失。
- 优化关于弹窗在浅色/深色主题下的文本、边框和卡片背景对比度，避免关键信息在浅色主题中可读性不足。

### 发布

- 同步更新 Web 与桌面端版本号到 `2.4.1`，用于触发 `v2.4.1` 修复版本 Release 和双服务器部署。

## v2.4.0 - 2026-05-05

### 重点更新

- 新增 SenseNova U1 Fast Provider 支持，可接入商汤 SenseNova 图像生成服务，并支持 2K 分辨率、常用比例预设和专属响应参数。
- 新增 Seedream Provider 支持，可接入火山方舟 Doubao Seedream 模型，并提供 1K/2K 分辨率、PNG/JPEG 输出、水印开关、组图生成、Seed、Guidance Scale、提示词优化和联网搜索等专属参数。
- 完善 Provider-aware 参数控制架构：编辑表单会根据当前供应商自动展示对应参数，OpenAI 保留质量、输出格式、背景、审核等通用控制，SenseNova 与 Seedream 展示各自文档化的高级选项。
- 系统设置面板新增 SenseNova/Seedream 配置区块，API Key 与 Base URL 支持显示/隐藏切换，配置优先级保持为 UI 设置 > `.env` > 系统默认。

### Provider 生态

- 新增 OpenAI-compatible Provider Adapter，统一多供应商图像请求适配层，为后续扩展更多兼容 OpenAI API 的图像服务打基础。
- 新增 Provider Credential Config 与 Provider Options Helpers，集中处理各供应商默认端点、运行时凭证、模型能力标记、请求参数合并和参数校验。
- 模型注册表接入多供应商模型，模型选择流程先区分 Provider，再展示对应模型，降低模型列表随供应商增加后的筛选成本。
- 分享链接支持携带 Provider 配置，接收者可选择仅本次使用、保存到本地设置或忽略分享中的 API 配置。

### 交互体验

- 优化移动端提示词模板弹窗排版，让搜索、模板列表和底部操作区在小屏幕上更紧凑。
- 历史记录移动端操作按钮改为更省空间的布局，常用动作在窄屏下更容易触达。
- 精简图片表单辅助说明和关于弹窗文案，减少非关键文本对主流程的干扰。

### 稳定性与错误处理

- 强化客户端直连模式错误提示，API 请求失败时返回更清晰的错误类型和说明，便于定位 CORS、认证或端点配置问题。
- 使用本地系统字体令牌，减少外部字体依赖和首屏网络请求，提升加载稳定性。

### 文档与发布

- README 补充 SenseNova/Seedream 配置说明，新增相关环境变量示例和默认 Base URL 说明。
- `.env.example` 新增 SenseNova 与 Seedream 配置模板，便于部署前快速填写供应商凭证。
- 同步更新 Web 与桌面端版本号到 `2.4.0`，确保 `v2.4.0` 标签触发的 GitHub Release 和桌面端构建流程通过版本一致性校验。
- 新增根目录发布规范文档，沉淀版本更新、tag 推送、GitHub Actions 构建和双服务器部署的标准流程。

### 升级注意事项

- 如需使用 SenseNova 或 Seedream，请在系统设置或 `.env.local` 中配置对应 API Key；Base URL 留空时会使用系统默认端点。
- Provider-aware 参数面板会随供应商切换动态变化，升级后建议验证 OpenAI、SenseNova、Seedream 的模型选择、参数展示、生成和分享链路。
- 如已启用 `CLIENT_DIRECT_LINK_PRIORITY=true`，非官方 Provider Base URL 会继续触发客户端直连优先策略，请确认目标服务支持浏览器 CORS。

## v2.3.1 - 2026-05-04

### 重点更新

- 优化提示词模板弹窗在移动端的排版，让搜索、模板列表和底部操作区在小屏幕上更紧凑，减少内容被控件挤压的情况。
- 历史记录移动端操作按钮改为更省空间的布局，常用动作在窄屏下更容易触达，同时避免按钮区域占用过多横向空间。
- 简化首页顶部控件布局，移除滚动显隐的额外状态逻辑，让标题、设置入口和操作按钮在不同屏幕宽度下保持更稳定的位置。

### 稳定性与发布

- 同步更新 Web 与桌面端版本号到 `2.3.1`，确保 `v2.3.1` 标签触发的 GitHub Release 和桌面端构建流程通过版本一致性校验。

## v2.3.0 - 2026-05-04

### 重点更新

- 全屏图片预览升级为跨图片的相册式查看体验，生成结果、历史记录和输出区多图都能在同一预览中左右滑动、拖拽或使用方向键切换。
- 新增可取消的图片切换过渡：拖拽不足阈值会平滑回弹，持续越过阈值才切换到相邻图片，减少误触切图。
- 优化移动端缩放计算，在小屏幕上按视口和图片尺寸动态限制初始缩放，打开大图时更容易完整浏览。

### 交互体验

- 抽象 ZoomViewer gallery 手势辅助逻辑，放大后优先拖拽查看图片细节，横向越过边界后再触发跨图切换。
- 历史面板、图片输出区和编辑表单统一接入多图预览上下文，发送到编辑、历史查看和当前生成结果之间的预览行为更一致。

### 构建与部署

- Dockerfile 与 docker-compose 支持构建时传入运行环境变量，便于在容器镜像构建阶段注入生产配置并保持部署脚本稳定。
- README 补充相册式切换交互说明，便于用户了解多图预览、拖拽过渡和键盘切换行为。

### 稳定性与测试

- 新增 ZoomViewer gallery 手势与缩放逻辑测试，覆盖拖拽阈值、可取消切换、边界判定和移动端缩放计算等关键路径。
- 同步更新 Web 与桌面端版本号到 `2.3.0`，确保 tag 驱动的 GitHub Release 和桌面端构建流程通过版本一致性校验。

## v2.2.2 - 2026-05-03

### 重点更新

- 新增 Umami 统计分析配置，部署时可通过 `UMAMI_SCRIPT_URL` 和 `UMAMI_WEBSITE_ID` 注入统计脚本；未完整配置时不会加载，避免误发请求。
- 全屏图片预览支持手机相册式多图切换：左右拖拽时会先露出相邻图片，松手距离不足会平滑回弹取消切换，持续拖过阈值才缓动切到上、下一张；放大后仍会先拖拽查看细节，越过水平边界后再触发切换，并支持键盘左右方向键。
- 提示词润色链路增强了异常解析与结果归一化，兼容更多 OpenAI-compatible 响应格式，并在失败时给出更清晰的提示。
- 安全分享链接支持在复制时附带内联解密密码，接收者打开完整链接即可自动解密，同时仍会清理地址栏中的敏感片段。

### 配置与分享

- 共享配置在直连模式下新增专用辅助逻辑，临时分享配置会在运行时生效，减少保存到本地设置前后的状态不一致。
- 简化安全分享密码的校验规则，允许更轻量的分享密码，降低临时协作场景的使用门槛。
- 设置面板对敏感字段的浏览器自动填充做了隔离处理，减少 API Key 或密码被误填到不相关输入框的可能性。

### 稳定性与测试

- 补充 API 错误处理、提示词润色、分享加密、URL 参数和共享配置相关测试，覆盖第三方接口异常、直连配置和加密分享的关键边界情况。
- 同步更新 Web 与桌面端版本号到 `2.2.2`，确保 tag 驱动的 GitHub Release 和桌面端构建流程通过版本一致性校验。

## v2.2.0 - 2026-05-03

### 重点更新

- 新增安全分享链接能力，可选择分享提示词、模型、API Base URL、自动生成开关等配置，并支持使用密码加密整段分享数据。
- 新增接收者配置保存选择，打开分享链接时可选择仅本次使用、保存到本地设置或忽略分享中的 API 配置。
- 新增提示词润色功能，可在输入区一键优化提示词，并可独立配置润色模型的 Base URL、API Key、模型 ID 和系统提示词。

### 交互体验

- 清空历史记录改为二次确认弹窗，并在清空前做数据归一化处理，降低误操作和旧数据结构带来的风险。
- 关于信息增加 GitHub 入口，网址改为生产站点 `img-playground.anzz.site`，展示文本更短，便于在一行内阅读。
- 编辑表单移除额外的 overscroll 限制，长内容滚动时更贴近页面整体滚动行为。

### 稳定性与发布

- 增加 URL 参数、加密分享、服务端 URL 安全校验、提示词润色和历史清理相关测试，覆盖分享与配置链路的关键边界情况。
- Docker 构建上下文排除本地产物目录，减少部署上传体积和误带本地缓存的可能性。
- 同步更新 Web 与桌面端版本号到 `2.2.0`，确保 tag 驱动的桌面构建发布流程能够通过版本一致性校验。

## v2.1.1 - 2026-05-02

### 重点更新

- 提示词输入区进一步加高，长提示词编辑时有了更充足的输入空间。
- 提示词工具区继续收敛为更克制的三按钮布局，桌面与移动端都保持右对齐且不再喧宾夺主。
- 新增提示词历史记录、模板搜索与清空能力，方便快速复用常用提示词。

### 配置与稳定性

- 系统设置增加提示词历史保留条数配置，并对历史数据做了归一化处理。
- 统一更新 Web 与桌面端版本号，确保发布流程的版本校验一致。

## v2.1.0 - 2026-05-02

### 重点更新

- 新增图片代理接口，支持服务端拉取跨域图片并转入应用存储链路，解决历史图片 URL 在编辑、保存和再次使用时的跨域限制。
- 重构图片保存逻辑，生成结果可携带图片 URL 并写入历史记录，为远程图片、代理图片和本地存储的统一处理打基础。
- 新增滚动感知的右上角控件显示策略，向下浏览内容时自动收起操作按钮，回到顶部后恢复显示，减少画布区域遮挡。
- 排队中的图片任务现在也可以取消，任务尚未开始执行时会被直接移出队列，避免无效请求继续占用任务队列。

### 交互体验

- 设置面板抽象统一的 ProviderSection 区块，OpenAI、Gemini 和自定义配置的呈现方式更一致。
- 调整提示词模板和顶部标题文案，首页信息层级更清晰，常用入口更容易识别。
- 优化历史面板、任务卡片和任务追踪器细节，使任务状态、图片 URL 和取消操作的反馈更连贯。

### 稳定性与兼容性

- 强化图片代理和下载失败时的错误类型与提示，接口会返回更可读的失败原因。
- 更新 API 图片生成流程，让文件系统存储、IndexedDB 存储和 URL 化图片路径更稳定地协同工作。
- 补充历史记录类型字段，兼容图片 URL 与已有 base64、本地路径历史数据。

### 升级注意事项

- 本版本更新了历史图片的存储字段，发布后建议验证“生成图片 → 历史查看 → 发送到编辑”的完整链路。
- 图片代理会从服务端拉取外部图片，请确认生产环境允许访问目标图片域名，并关注服务器带宽与超时策略。

## v2.0.0 - 2026-05-01

### 重点更新

- 新增 Tauri 桌面应用支持，发布流程可为 macOS、Windows 和 Linux 自动构建安装包。
- 新增 Gemini 图像 Provider，并引入 Provider-aware 模型注册与任务执行架构，为后续多模型扩展打基础。
- 新增提示词模板与斜杠命令系统，可通过 `/` 快速插入常用图片生成/编辑模板，并支持键盘导航。
- 新增并发任务系统，支持任务持久化、任务卡片、任务追踪器和毫秒级耗时展示。
- 新增 UI 系统设置面板，支持 API Key、Base URL、图片存储模式、连接模式和自定义模型配置热更新。

### 交互体验

- 历史图片支持双击全屏预览，并可从历史记录或预览中快速“发送到编辑”。
- 图片查看器支持点击缩放、滚轮缩放、双指缩放、拖拽移动、点击外部关闭和 ESC 退出。
- 编辑模式支持全局剪贴板粘贴和全局拖拽上传，图片输入路径更顺手。
- 设置页、主工作区、历史面板和辅助对话框完成移动端与亮色/暗色主题适配。

### 稳定性与错误处理

- 强化 API 错误处理与连接策略，新增环境变量示例，第三方中转/直连场景提示更清晰。
- 修复取消任务时缺失 AbortController、设置保存/重置后的状态同步、页面缩放误触和右键拖拽误触等问题。
- 修复缩放查看器拉伸变形、点击外部关闭、垂直居中和布局标签换行等 UI 问题。

### 国际化与文档

- 完成整体 UI 简体中文翻译，并修复中文字符串引号问题。
- README 补充连接模式、系统设置和构建说明。

### 构建与发布

- 新增跨平台 Tauri 构建工作流，并在 v2.0.0 中升级为 tag 驱动发布：推送 `v*` 标签后会校验版本、生成 GitHub Release 描述并上传桌面安装包。
- 构建流程新增版本一致性检查、Lint、TypeScript 类型检查、Web 构建预检和工作流产物上传，减少发布失败和描述过简的问题。

### 升级注意事项

- 生成/编辑表单已统一为新的工作流，历史使用习惯可能略有变化。
- 图像任务统一经过 Provider-aware 执行层，建议同时验证 OpenAI 与 Gemini 配置。
- 新增任务持久化与主题系统后，如遇到旧配置显示异常，可在系统设置中重置相关配置。
