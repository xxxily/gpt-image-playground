{
    "$schema": "../gen/schemas/desktop-schema.json",
    "identifier": "desktop",
    "description": "enables desktop-only updater and process permissions",
    "windows": ["main"],
    "platforms": ["macOS", "windows", "linux"],
    "permissions": ["process:allow-restart", "updater:default"]
}
