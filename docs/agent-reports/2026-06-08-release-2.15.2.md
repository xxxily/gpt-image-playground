# 2.15.2 版本发布报告

| 字段 | 内容 |
| --- | --- |
| 日期 | 2026-06-08 |
| 状态 | 进行中 (Partial) |
| 相关请求 | 根据 `RELEASE_PROCESS.md` 规范发布 `2.15.2` 版本；`142` 服务器已经挂掉，本次忽略该服务器发版。 |
| 相关文档 | [RELEASE_PROCESS.md](../../RELEASE_PROCESS.md)、[CHANGELOG.md](../../CHANGELOG.md)、[批量 AI 配置与历史分辨率修复](./2026-06-08-batch-ai-history-fixes.md) |
| 改动范围 | 发布版本号、中文变更日志、本次 agent 发版报告、GitHub tag、GitHub Actions、GitHub Release 资产、`129` 服务器部署和发布后验证结果。 |
| 提交状态 | 发布提交、tag、推送和部署尚未完成。 |

## 范围核对

| 请求目标 | 实际结果 | 证据 | 状态 |
| --- | --- | --- | --- |
| 按 `RELEASE_PROCESS.md` 发布 2.15.2 | 已完成发布前状态核对、版本号/changelog 更新和本地发布校验；发布提交、tag、推送、Actions、Release 资产检查和 `129` 部署待完成。 | `package.json`、`package-lock.json`、`src-tauri/tauri.conf.json`、`src-tauri/Cargo.toml`、`CHANGELOG.md` | 部分完成 (Partial) |
| 忽略 `142` 服务器发版 | 本次发布范围调整为仅部署 `129`；`142` 部署脚本和 `ora.anzz.top` 验证项会跳过。 | 用户本轮请求；本报告；`CHANGELOG.md` 的 `v2.15.2` 小节 | 已完成 (Completed) |
| 版本文件一致 | Web、package-lock、Tauri、Cargo 和本地 Cargo lock 根包版本已同步到 `2.15.2`；Android identifier 为 `site.anzz.gptimageplayground`。 | 版本一致性检查输出 | 已完成 (Completed) |
| 中文变更日志 | 已插入 `v2.15.2 - 2026-06-08` 小节，覆盖批量/润色/图生文模型保留、历史分辨率显示、批量阈值和跳过 142。 | `CHANGELOG.md` | 已完成 (Completed) |
| 本地校验 | 已完成发布规范要求的 npm、Web、桌面、Rust 和版本一致性检查。 | 验证表 | 已完成 (Completed) |
| GitHub Release / Actions | 待发布提交和 tag 推送后检查。 | 待回填 | 部分完成 (Partial) |
| `129` 服务器部署 | 待 GitHub Actions 触发后执行 `scripts/deploy-129.sh`。 | 待回填 | 部分完成 (Partial) |
| `142` 服务器部署 | 本次按用户要求跳过，不执行 `scripts/deploy.sh`。 | 用户明确说明 `142` 已挂掉；本报告 | 不适用 (N/A) |

## 问题与解决

| 问题 | 解决办法 | 剩余风险 |
| --- | --- | --- |
| 标准流程默认要求工作区干净 | 当前存在发布前已有的无关社区推广文档改动；本次仅暂存和提交发布相关文件，避免混入无关变更。 | `git status` 会继续显示这些无关未提交文件，不代表发布提交包含它们 |
| 标准流程默认包含 `142` 与 `129` 两台服务器部署 | 本次遵循用户最新约束，跳过 `142`，仅执行 `129` 部署和 `img-playground.anzz.site` 访问验证。 | `142` 对应域名不会在本次发版得到更新；服务器恢复后需要按 `v2.15.2` tag 单独补发 |
| `src-tauri/Cargo.lock` 被 `.gitignore` 忽略 | 本地同步到 `2.15.2` 并纳入版本一致性检查；发布提交不包含该忽略文件。 | 如果 CI 未来改为强制跟踪 Cargo lock，需要先调整 `.gitignore` 和提交策略 |

## 验证

| 检查项 | 命令或场景 | 结果 |
| --- | --- | --- |
| 发布前 git 状态 | `rtk git status --short --branch` | 部分通过：`master` 跟踪 `origin/master`，本地超前 1 个修复提交；存在发布前已有无关文档改动，未纳入发布提交 |
| 最新 tag | `rtk git tag --sort=-v:refname` | 通过：上一版本为 `v2.15.1` |
| 变更范围 | `rtk git log v2.15.1..HEAD --oneline` | 通过：包含 `666feac fix: preserve batch AI defaults and history size` |
| 版本一致性检查 | `rtk node -e "..."` | 通过：`package`、`lock`、`lockRoot`、`tauri`、`cargo`、本地 `cargoLock` 均为 `2.15.2`，Android identifier 为 `site.anzz.gptimageplayground` |
| `npm ci` | `rtk npm ci` | 通过：安装 583 个包，0 vulnerabilities；仍有既有 `prebuild-install@7.1.3` deprecated 提示 |
| `npm run secret-scan` | `rtk npm run secret-scan` | 通过：tracked files 未发现疑似 secret |
| `npm run release:env-check` | `rtk npm run release:env-check` | 通过：报告 `.env.example`、`.env.production` 为 tracked，`.env.local` 和历史备份为 untracked；未打印 env 内容 |
| `npm run audit:prod` | `rtk npm run audit:prod` | 通过：0 vulnerabilities |
| `npm run typecheck` | `rtk npm run typecheck` | 通过 |
| `npm run lint` | `rtk npm run lint` | 通过 |
| `npm run test` | `rtk npm run test` | 通过：102 个测试文件，993 个测试 |
| `npm run build` | `rtk npm run build` | 通过；仍有既有 `src/app/api/history-assets/route.ts` broad-pattern Turbopack warning |
| `npm run build:desktop` | `rtk npm run build:desktop` | 通过：桌面静态构建只输出 `/` 和 `/_not-found` |
| `npm run rust:test` | `rtk npm run rust:test` | 通过：83 个 Rust 单元测试 |
| `npm run rust:clippy` | `rtk npm run rust:clippy` | 通过：`-D warnings` 无错误 |
| `npm run rust:audit` | `rtk npm run rust:audit` | 通过，退出码 0；报告 19 条允许的上游依赖 warning |

## 后续建议

- `142` 服务器恢复后，从 `v2.15.2` tag 单独补跑 `scripts/deploy.sh`，并补验 `img-playground.ora.anzz.top` 根页面、后台、展示后台和缓存头。
