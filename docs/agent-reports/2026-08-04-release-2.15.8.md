# v2.15.8 发布与构建报告

| 字段 | 内容 |
| ---- | ---- |
| 日期 | 2026-08-04 |
| 状态 | 部分完成 (Partial)：专题产品闭环仍缺真实 AI 案例、外部 HTTPS 媒体主动探测、非专业用户测试和真机安装验收；`v2.15.8` 发布、Action 产物和 129 部署均已完成 (Completed)。 |
| 相关请求 | 专业、全面地梳理并实现专题案例能力，完成后发布新的小版本、推送并触发 Action 构建产物 |
| 相关文档 | [专题需求](../requirements/SHOWCASE_TOPICS_AND_GUIDED_CREATION_REQUIREMENTS.md)、[专题持续报告](./2026-08-03-showcase-topics-guided-creation-requirements.md)、[后台使用手册](../展示内容与后台管理使用手册.md)、[发布流程](../../RELEASE_PROCESS.md) |
| 改动范围 | 托管媒体、专题匿名漏斗、未来 recipe 只读降级、后台结构化配方、SQLite 事务一致性、i18n、需求与发布文档、版本文件 |
| 提交状态 | 功能提交 `ef1fa43`、发布提交 `23f9cba` 和 annotated tag `v2.15.8` 已推送；本报告将在发布收尾提交中单独提交并推送，tag 保持指向 `23f9cba`。用户既有的任务服务、社区调研、PSD 研究和索引改动未纳入。 |

## 范围核对

| 请求目标 | 实际结果 | 证据 | 状态 |
| -------- | -------- | ---- | ---- |
| 专题需求与产品闭环 | 已完成需求细化及核心功能实现；默认 6 个专题、24 个案例仍是明确占位媒体 | 专题需求、持续报告、功能提交 `ef1fa43` | 部分完成 (Partial) |
| 后台专题运营 | 已支持托管媒体上传/指派/删除保护、主要结构化配方字段、发布快照、审计和 30 天漏斗 | `/admin/showcases`、专题 API/SQLite 模块、单元与浏览器测试 | 已完成 (Completed) |
| 安全与兼容 | 托管媒体健康复查、写事务一致性、扩展/旧客户端目录协商、未来 recipe 只读隔离均已实现 | server/client tests、浏览器 route mock | 已完成 (Completed) |
| 发布新补丁版本 | Web、package-lock、Tauri、Cargo 和 Changelog 已同步到 `2.15.8`；发布提交与 annotated tag 已推送 | `23f9cba`、`v2.15.8`、`origin/master` | 已完成 (Completed) |
| Action 构建产物 | tag push 已触发 Release workflow；7 个 jobs 全部成功，正式 Release 共 15 个资产 | Run `30874164460`、GitHub Release `v2.15.8` | 已完成 (Completed) |
| 129 生产部署 | 从 release tag 独立 worktree部署；`current`、systemd、HTTP、ETag、SQLite 与回滚点均验证通过 | `20260804112334-v2.15.8`、129 操作日志 | 已完成 (Completed) |

## 问题与解决

| 问题 | 解决办法 | 剩余风险 |
| ---- | -------- | -------- |
| 工作区包含其他用户/代理的未提交文档与研究文件 | 功能提交和发布提交均使用精确文件列表暂存，发布/部署使用 release commit 或 tag 的独立 worktree | 无关改动继续留在本地，后续操作仍不得误提交 |
| `npm audit` 前两次遇到 registry TLS 失败 | 保留失败证据并重试；第三次成功完成生产依赖审计 | 外部 registry 网络仍可能在 Action 或后续部署时波动 |
| Rust advisory DB 在线更新失败 | 使用本地 stale advisory DB 完成离线扫描 | `quick-xml 0.39.2` 仍命中 `RUSTSEC-2026-0194`、`RUSTSEC-2026-0195`，兼容修复受 Tauri 间接依赖与 Rust MSRV 约束 |
| 浏览器并行读取同一 Playwright 会话导致 CLI 会话退出 | 重新创建隔离会话并串行完成未来 recipe 深链检查 | 属于测试工具会话竞争，不是产品运行时错误 |
| 首次生产 SQLite 只读脚本把相对 `ADMIN_DATABASE_PATH` 错按 release 目录解析 | 按 `src/lib/server/db.ts` 复刻真实规则：相对路径落到 `/tmp/gpt-image-playground/`，`~/` 展开为远程用户主目录；再使用 bundled Node 只读检查 | 已正确验证三张增量表和 `quick_check`；脚本未修改生产数据 |

## 验证

| 检查项 | 命令或场景 | 结果 |
| ------ | ---------- | ---- |
| 安装与安全门 | `rtk npm ci`；`rtk npm run secret-scan`；`rtk npm run release:env-check`；`rtk npm run audit:prod` | 通过；生产依赖 0 vulnerabilities |
| TypeScript / ESLint | `rtk npm run typecheck`；`rtk npm run lint` | 通过 |
| 全量测试 | `rtk npm run test` | 121 个文件、1093 项通过 |
| 定向专题测试 | `rtk npm run test -- --run 'src/app/api/showcase-media/[id]/route.test.ts' src/app/api/showcases/route.test.ts src/lib/server/showcase/analytics.test.ts src/lib/server/showcase/media.test.ts src/lib/server/showcase/public.test.ts src/lib/showcase-admin-draft.test.ts src/lib/showcase-analytics.test.ts src/lib/showcase-client.test.ts src/lib/showcase-recipe.test.ts src/lib/showcase.test.ts` | 10 个文件、53 项通过 |
| Web / Desktop | `rtk npm run build`；`rtk npm run build:desktop` | 通过 |
| Rust | `rtk npm run rust:test`；`rtk npm run rust:clippy`；`rtk cargo metadata --manifest-path src-tauri/Cargo.toml --locked --format-version 1` | 83 项测试通过；Clippy 与锁文件检查通过 |
| Rust audit | 在线 `cargo audit`；`rtk cargo audit --no-fetch --stale --file src-tauri/Cargo.lock` | 在线更新失败；离线扫描完成但因两项既有 `quick-xml` advisory 非零退出，另有 21 个允许 warning |
| 前台浏览器 | 1440×900 浅色；1280×800 深色；390×844 浅色/深色；reduced-motion；老照片详情、案例、Guide | 无横向溢出，console 0 error / 0 warning；Guide 不自动提交；reduced-motion 无持续动画 |
| 后台浏览器 | 临时 SQLite + owner；媒体上传/指派/删除、指标、结构化字段；390×844 深色 | 自定义尺寸、推荐模型保存成功；引用媒体删除 409；顶层确认 Dialog 单实例；成功删除后返回媒体库；无横向溢出 |
| 未来 recipe | 请求拦截将一个案例升级到 recipe v2 并加入未知脚本字段 | 卡片/详情只读降级；输入要求未知；deep-link 和工作台不执行未知字段 |
| Git 差异 | `rtk git diff --cached --check`、精确 staged 文件检查 | 通过；未混入用户无关改动 |
| GitHub Actions / Release | `rtk gh run view 30874164460`；`rtk gh release view v2.15.8` | Run 完成且 success，7 个 jobs 成功；Release 非 draft/非 prerelease，15 个资产齐全 |
| 129 HTTP / 版本 | `/`、`/topics`、`/admin`、`/admin/showcases`、`/api/showcases`；读取当前 package version | 200、200、307、307、200；后台跳转 `/admin/login`；线上版本 `2.15.8` |
| 129 目录 API | 解析 `/api/showcases` 并使用 ETag 发起条件请求 | 6 个专题、24 个案例、56 个媒体；条件请求 304 |
| 129 SQLite | 按服务端路径规则解析生产库，使用 bundled `better-sqlite3` 只读查询 | `showcase_assets`、`showcase_publication_assets`、`showcase_events` 均存在；当前均 0 行；`PRAGMA quick_check = ok` |
| 129 环境与回滚 | 只检查认证 secret 存在性、最新环境备份和上一 release | 两项 secret 均保留；备份 `.env.20260804113001`；回滚目录 `20260804084729-v2.15.7` |
| 161 构建证据 | 查询 artifact、SHA-256、大小和 source 环境文件 | 48,698,129 bytes；SHA-256 `7d6a60c0ea2dd7a49ddbd5ae7b94254ff47a548f336bc5465126b2f78a2ab616`；`.env.production` / `.env.local` 均不存在 |

## 发布、Action 与部署

| 项目 | 状态 |
| ---- | ---- |
| 目标版本 | `2.15.8`（基线 `2.15.7`） |
| 功能提交 | `ef1fa43 feat(showcase): add managed media and funnel insights` |
| 发布提交 / tag / push | `23f9cba chore: release v2.15.8`；annotated tag `v2.15.8`；`master` 和 tag 均已推送，tag 保持指向发布提交 |
| GitHub Actions | Run `30874164460`，状态 `completed / success`，7 个 jobs 全部成功 |
| Release 资产 | 正式发布，非 draft、非 prerelease；15 个资产，包括 macOS DMG/app updater、Windows EXE/MSI、Linux deb/rpm/AppImage、签名、release-signed Android APK 和 `latest.json` |
| 129 部署 | `current` → `/root/work/gpt-image-playground/releases/20260804112334-v2.15.8`；systemd `active`；Node `20.20.2`；161 Docker 构建；SQLite `quick_check = ok` |
| 142 部署 | 明确跳过；按 `RELEASE_PROCESS.md` 当前暂停 |
| Outline / 服务器操作日志 | 需求 `/doc/gpt-image-playground-U4dlPsf2LV`；持续报告 `/doc/5lit6aky5qgi5l6l5lio5byv5a85byp5yib5l2c6io95yqb5a6e546w5lio5yr5bid5oql5zgk-RIyic3iPPT`；发布报告 `/doc/gpt-image-playground-v2158-hDVXX80dEC`；129 `/doc/gpt-image-playground-v2158-129-oxWHSApp9m`；161 `/doc/gpt-image-playground-v2158-161-2oR6uARHmD`。本地日志位于对应 129/161 notes 仓库。 |

## 明确跳过与后续建议

- 当前会话没有可调用的内置图片生成能力，因此没有用色块或未经核验的素材冒充 24 个真实 AI 案例。
- 未执行 5 名非专业用户的 30/60 秒可用性测试，缺少真实受试者条件。
- 未安装 macOS、Windows、Linux 桌面包或 Android APK；Action 构建成功不能替代安装级真机验收。
- 外部 HTTPS 媒体仍需增加 DNS/重定向复检、MIME、体积和解码探测；多实例部署应使用共享卷或对象存储。
