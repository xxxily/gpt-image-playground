# 2.15.1 版本发布报告

| 字段     | 内容                                                                                                                                                 |
| -------- | ---------------------------------------------------------------------------------------------------------------------------------------------------- |
| 日期     | 2026-06-06                                                                                                                                           |
| 状态     | 已完成 (Completed)                                                                                                                                   |
| 相关请求 | 根据 `RELEASE_PROCESS.md` 规范发布 `2.15.1` 版本；`142` 服务器已经挂掉，本次忽略该服务器发版。                                                       |
| 相关文档 | [RELEASE_PROCESS.md](../../RELEASE_PROCESS.md)、[CHANGELOG.md](../../CHANGELOG.md)、[运行任务详情报告](./2026-06-05-task-run-details.md)、Outline 操作记录 `/doc/gpt-image-playground-v2151-129-wHfHbeqTBu` |
| 改动范围 | 发布版本号、中文变更日志、Better Auth 安全升级、本次 agent 发版报告、GitHub tag、GitHub Actions、GitHub Release 资产、`129` 服务器部署和发布后验证结果。 |
| 提交状态 | 发布提交 `d10a0aafe20cbef4bce5bda7aa110aac076ebc38` 已推送到 `master`，tag `v2.15.1` 已推送；本报告最终回填为后续文档提交。                          |

## 范围核对

| 请求目标                            | 实际结果                                                                                             | 证据                                                                                                    | 状态               |
| ----------------------------------- | ---------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------- | ------------------ |
| 按 `RELEASE_PROCESS.md` 发布 2.15.1 | 已完成发布前状态核对、变更归类、版本号/changelog 更新、本地发布校验、发布提交、tag 推送、GitHub Actions、Release 资产检查和 `129` 部署。 | `package.json`、`package-lock.json`、`src-tauri/tauri.conf.json`、`src-tauri/Cargo.toml`、`CHANGELOG.md` | 已完成 (Completed) |
| 忽略 `142` 服务器发版               | 本次发布范围已调整为仅部署 `129`；`142` 部署脚本和 `ora.anzz.top` 验证项会跳过。                      | 用户本轮请求；本报告；`CHANGELOG.md` 的 `v2.15.1` 小节                                                  | 已完成 (Completed) |
| 版本文件一致                        | Web、package-lock、Tauri、Cargo 与本地 Cargo lock 根包版本均已同步到 `2.15.1`，Android 包名不含连字符。 | 版本一致性检查输出                                                                                      | 已完成 (Completed) |
| 中文变更日志                        | 已插入 `v2.15.1 - 2026-06-06` 小节，覆盖运行任务详情、脱敏、安全升级、文档发布和 142 跳过说明。       | `CHANGELOG.md`                                                                                          | 已完成 (Completed) |
| 本地校验                            | 已完成发布规范要求的 npm、Web、桌面、Rust 和版本一致性检查；`better-auth` 审计失败项已升级修复。     | 验证表                                                                                                  | 已完成 (Completed) |
| GitHub Release / Actions            | Release workflow 已全部成功，Release 已发布为正式版本并上传桌面端、Android APK、签名文件和 `latest.json` 资产。 | <https://github.com/xxxily/gpt-image-playground/actions/runs/27028434690>、<https://github.com/xxxily/gpt-image-playground/releases/tag/v2.15.1> | 已完成 (Completed) |
| `129` 服务器部署                    | 已使用 161 Docker 构建 Linux x64/glibc 2.28 runtime 包并切换 `129` current 到 `v2.15.1`。脚本首次在 env 上传时遇到 SSH 断连，随后复用已上传 artifact 手动续跑远端激活逻辑完成。 | `/root/work/gpt-image-playground/releases/20260606010448-v2.15.1`、`https://img-playground.anzz.site` | 已完成 (Completed) |
| `142` 服务器部署                    | 本次按用户要求跳过，不执行 `scripts/deploy.sh`。                                                     | 用户明确说明 `142` 已挂掉；本报告                                                                       | 不适用 (N/A)       |

## 问题与解决

| 问题                                           | 解决办法                                                                                 | 剩余风险                                                                         |
| ---------------------------------------------- | ---------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------- |
| 标准流程默认包含 `142` 与 `129` 两台服务器部署 | 本次遵循用户最新约束，跳过 `142`，仅执行 `129` 部署和 `img-playground.anzz.site` 访问验证 | `142` 对应域名不会在本次发版得到更新；服务器恢复后需要按 `v2.15.1` tag 单独补发 |
| 本地分支比 `origin/master` 超前 1 个功能提交   | 将该提交纳入 `v2.15.1` 发布范围，并在 changelog 中发布其用户可感知变化                   | 无明显剩余风险                                                                   |
| 生产依赖审计发现 `better-auth` 高危公告         | 将 `better-auth`、`@better-auth/drizzle-adapter` 和 `@better-auth/core` override 升级到 `1.6.14`，并重新生成 lockfile | 已通过 `npm ci`、`npm run audit:prod`、typecheck、lint、test、build、build:desktop |
| 129 SSH 在 env 上传阶段断连                     | 确认 `current` 仍为 `v2.15.0` 且服务 active 后，复用已上传的 `v2.15.1` runtime artifact，按 `deploy-129.sh` 远端激活逻辑补传 env、解包、切换软链并重启 systemd | 后续可给 `deploy-129.sh` 的 `upload_file` 增加重试；本次发布已完成并验证 |

## 验证

| 检查项 | 命令或场景 | 结果 |
| ------ | ---------- | ---- |
| 发布前 git 状态 | `rtk git status --short --branch` | 通过：`master` 跟踪 `origin/master`，起始工作区干净且本地超前 1 个提交 |
| 最新 tag | `rtk git tag --sort=-v:refname` | 通过：上一版本为 `v2.15.0` |
| 变更范围 | `rtk git log v2.15.0..HEAD --oneline --stat` | 通过：包含 `059017b docs: update v2.15.0 release report` 和 `04b4fa0 feat: add expandable task run details` |
| 版本一致性检查 | `rtk node -e "..."` | 通过：`package`、`lock`、`lockRoot`、`tauri`、`cargo`、本地 `cargoLock` 均为 `2.15.1`，Android identifier 为 `site.anzz.gptimageplayground` |
| `npm ci` | `rtk npm ci` | 通过：安全升级后干净安装 583 个包，0 vulnerabilities；仍有 `prebuild-install@7.1.3` deprecated 提示 |
| `npm run secret-scan` | `rtk npm run secret-scan` | 通过：tracked files 未发现疑似 secret |
| `npm run release:env-check` | `rtk npm run release:env-check` | 通过：报告 `.env.example`、`.env.production` 为 tracked，`.env.local` 和历史备份为 untracked；未打印 env 内容 |
| `npm run audit:prod` | `rtk npm run audit:prod` | 首次失败于 `better-auth 1.6.10` 高危公告；升级到 `1.6.14` 后通过，0 vulnerabilities |
| `npm run typecheck` | `rtk npm run typecheck` | 通过 |
| `npm run lint` | `rtk npm run lint` | 通过 |
| `npm run test` | `rtk npm run test` | 通过：102 个测试文件，988 个测试 |
| `npm run build` | `rtk npm run build` | 通过；仍有既有 `src/app/api/history-assets/route.ts` broad-pattern Turbopack warning |
| `npm run build:desktop` | `rtk npm run build:desktop` | 通过：桌面静态构建只输出 `/` 和 `/_not-found` |
| `npm run rust:test` | `rtk npm run rust:test` | 通过：83 个 Rust 单元测试 |
| `npm run rust:clippy` | `rtk npm run rust:clippy` | 通过：`-D warnings` 无错误 |
| `npm run rust:audit` | `rtk npm run rust:audit` | 通过，退出码 0；报告 19 条允许的上游依赖警告 |
| GitHub Actions | `rtk gh run view 27028434690 --json status,conclusion,url,jobs` | 通过：workflow `completed/success`，metadata/web、Release notes、Android、Windows、Linux、macOS 和 Publish Release job 全部成功 |
| GitHub Release 资产 | `rtk gh release view v2.15.1 --json ...` | 通过：正式 Release，非 draft；15 个资产包含 macOS DMG/app tar/signature、Windows MSI/EXE/signature、Linux deb/rpm/AppImage/signature、Android APK 和 `latest.json` |
| `129` 部署 | `BETTER_AUTH_SECRET=<redacted> ADMIN_BOOTSTRAP_SECRET=<redacted> rtk bash scripts/deploy-129.sh`；SSH 断连后手动续跑 env 上传和远端激活 | 通过：artifact `gpt-image-playground-v2.15.1-20260606010448-linux-x64.tar.gz`，50M；build backend `161-docker`；native mode `linux-glibc228`；runtime 150M |
| `129` 服务状态 | `rtk ssh ... "systemctl is-active ...; readlink -f ..."` | 通过：`active`，current 指向 `/root/work/gpt-image-playground/releases/20260606010448-v2.15.1`，远端 package version 为 `2.15.1`，`BETTER_AUTH_SECRET` 和 `ADMIN_BOOTSTRAP_SECRET` 均存在 |
| `129` HTTP 验证 | `rtk curl -sI https://img-playground.anzz.site`、`/admin`、`/admin/promo`；`curl -L` final 检查 | 通过：根页面 HTTP 200；`/admin` 和 `/admin/promo` HTTP 307 到 `/admin/login`，跟随跳转后均为 200 |
| `129` 缓存头验证 | `rtk curl -sI https://img-playground.anzz.site \| rtk rg -i '^cache-control:'` | 通过：`Cache-Control: no-cache, no-store, must-revalidate` |
| 129 操作日志同步 | 本地日志 `/Users/blaze/work/github/notes/serverinfo/129_notes/OperationLog/2026/06/06/gpt-image-playground-v2.15.1-129.md`；Outline `操作记录` collection | 通过：创建 Outline 文档 `/doc/gpt-image-playground-v2151-129-wHfHbeqTBu` |
| `142` 部署与验证 | 按用户要求跳过 | 跳过 |

## 后续建议

- `142` 服务器恢复后，从 `v2.15.1` tag 单独补跑 `scripts/deploy.sh`，并补验 `img-playground.ora.anzz.top` 根页面、后台、展示后台和缓存头。
- 可给 `scripts/deploy-129.sh` 的 `upload_file` 增加小次数重试，降低 129 偶发 SSH 断连对部署流程的影响。
